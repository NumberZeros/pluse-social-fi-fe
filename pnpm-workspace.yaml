# Single-app repo — not a monorepo.
# pnpm 11+: project config (allowBuilds) must live here.
strictDepBuilds: false
allowBuilds:
  esbuild: true
  bigint-buffer: true
  bufferutil: true
  utf-8-validate: true
